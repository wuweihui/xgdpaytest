*** Settings ***
| Resource        | TradeResources.txt
| Test Setup      | connect and checkin pos
| Test Teardown   | disconnect and checkout pos

*** Variables ***



*** Keywords ***


*** Test Cases ***
correction with invalid voucher number and mac
|  | send pos message   | ${TEMPLATE_POSSETTLE}  | terminalId=${VALID TERMINALID}   | merchantId=${VALID MERCHANTID}  | operationData.correctionMsg.voucherNo=${INVALID VOUCHER NUMBER} | operationData.correctionMsg.tradeMAC=${INVALID TRADE MAC}
|  | response code should be  | ${RESPOND NOT CHECKIN}

correction with invalid mac
|  | ${amount}=         | Set Variable    | 000000003333
|  | pos trade          | ${amount}       | terminalId=${VALID TERMINALID}   | merchantId=${VALID MERCHANTID}
|  | ${voucherno}=      | get field       | voucherNo
|  | send pos message   | ${TEMPLATE_POSSETTLE}  | terminalId=${VALID TERMINALID}   | merchantId=${VALID MERCHANTID}  | operationData.correctionMsg.voucherNo=${voucherno} | operationData.correctionMsg.tradeMAC=${INVALID TRADE MAC}
|  | response code should be  | ${RESPOND NOT CHECKIN}

correction before checkin
|  | checkout pos       |   terminalId=${VALID TERMINALID}  |  merchantId=${VALID MERCHANTID}
|  | send pos message   | ${TEMPLATE_POSSETTLE}  | terminalId=${VALID TERMINALID}   | merchantId=${VALID MERCHANTID} 
|  | response code should be  | ${RESPOND NOT CHECKIN}